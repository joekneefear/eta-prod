fastapi>=0.95.0
pydantic>=2.0
uvicorn[standard]
python-dotenv
aiofiles>=23.0.0
# Optional Oracle driver (install only if you use the Oracle backend):
# python-oracledb  # Optional: install if using Oracle backend
pytest
httpx
